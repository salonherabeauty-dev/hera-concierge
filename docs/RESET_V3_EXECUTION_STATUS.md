# Hera Receptionist Reset v3

Status: implementation branch only; not user-certified.

The reset is restricted to a private shadow Preview. It uses one OpenAI GPT-5.6 Sol Max draft call and at most one bounded rewrite, preserves the approved Hera knowledge library, writes only editable candidates, requires a deliberate human send, performs no Timely writes and permits no automatic WhatsApp delivery.

No test link may be issued until the four isolated end-to-end proofs pass on the exact deployed commit.
